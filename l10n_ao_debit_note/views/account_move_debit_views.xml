<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="l10n_ao_account_move_view" model="ir.ui.view">
            <field name="name">account.move.form.debit.note</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <field name="partner_id" position="after">
                    <field name="is_debit_note" invisible="1"/>
                </field>
                <xpath expr="//div[@class='oe_title']" position="attributes">
                    <attribute name="attrs">{'invisible': [('is_debit_note', '=', True)]}</attribute>
                </xpath>
                <xpath expr="//div[@class='oe_title']" position="after">
                    <h5 attrs="{'invisible': [('is_debit_note', '=', False)]}">Nota de Débito</h5>
                    <div class="oe_title" attrs="{'invisible': [('is_debit_note', '=', False)]}">
                        <h1>
                            <span attrs="{'invisible': [('state', '!=', 'draft')]}">Draft</span>

                            <field name="debit_note"
                                   attrs="{'invisible': [('state', '=', 'draft')]}"
                                   readonly="1"
                                   force_save="1"/>
                        </h1>
                    </div>
                </xpath>
            </field>
        </record>
        <record id="view_move_tree_debit_note" model="ir.ui.view">
            <field name="name">account.move.tree.debit.note</field>
            <field name="model">account.move</field>
            <field name="arch" type="xml">
                <tree>
                    <header>
                        <button name="action_register_payment" type="object" string="Register Payment"
                                groups="account.group_account_user"
                                invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund', 'out_receipt', 'in_invoice', 'in_refund','in_receipt')"/>
                    </header>
                    <field name="made_sequence_hole" invisible="1"/>
                    <field name="debit_note" decoration-bf="1" decoration-danger="made_sequence_hole"/>
                    <field name="invoice_partner_display_name"
                           invisible="context.get('default_move_type') not in ('in_invoice', 'in_refund','in_receipt')"
                           groups="base.group_user" string="Vendor"/>
                    <field name="invoice_partner_display_name"
                           invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund','out_receipt')"
                           groups="base.group_user" string="Customer"/>
                    <field name="invoice_date" optional="show"
                           invisible="context.get('default_move_type') not in ('in_invoice', 'in_refund','in_receipt')"
                           string="Bill Date"/>
                    <field name="invoice_date" optional="show"
                           invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund','out_receipt')"
                           string="Invoice Date"/>
                    <field name="date" optional="hide" string="Accounting Date"/>
                    <field name="invoice_date_due" widget="remaining_days" optional="show"
                           attrs="{'invisible': [['payment_state', 'in', ('paid', 'in_payment', 'reversed')]]}"/>
                    <field name="invoice_origin" optional="hide" string="Source Document"/>
                    <field name="payment_reference" optional="hide"
                           invisible="context.get('default_move_type') in ('out_invoice', 'out_refund','out_receipt')"/>
                    <field name="ref" optional="hide"/>
                    <field name="invoice_user_id" optional="hide"
                           invisible="context.get('default_move_type') not in ('out_invoice', 'out_refund','out_receipt')"
                           string="Salesperson" widget="many2one_avatar_user"/>
                    <field name="activity_ids" widget="list_activity" optional="show"/>
                    <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"
                           optional="hide"/>
                    <field name="company_id" groups="!base.group_multi_company" invisible="1"/>
                    <field name="amount_untaxed_signed" string="Tax Excluded" sum="Total" optional="show"/>
                    <field name="amount_tax_signed" string="Tax" sum="Total" optional="hide"/>
                    <field name="amount_total_signed" string="Total" sum="Total" decoration-bf="1" optional="show"/>
                    <field name="amount_total_in_currency_signed" string="Total in Currency"
                           groups="base.group_multi_currency" optional="show"/>
                    <field name="amount_residual_signed" string="Amount Due" sum="Amount Due" optional="hide"/>
                    <field name="currency_id" groups="base.group_multi_currency" optional="hide"/>
                    <field name="company_currency_id" invisible="1"/>
                    <field name="to_check" optional="hide" widget="boolean_toggle"/>
                    <field name="payment_state" widget="badge" decoration-danger="payment_state == 'not_paid'"
                           decoration-warning="payment_state in ('partial', 'in_payment')"
                           decoration-success="payment_state in ('paid', 'reversed')"
                           attrs="{'invisible': [('payment_state', 'in', ('invoicing_legacy'))]}" optional="show"/>
                    <field name="state" widget="badge" decoration-success="state == 'posted'"
                           decoration-info="state == 'draft'" optional="show"/>
                    <field name="move_type" invisible="context.get('default_move_type', True)"/>
                </tree>
            </field>
        </record>
        <record id="view_move_form_debit_inherit_string" model="ir.ui.view">
            <field name="name">account.move.form.debit.inherit.string</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account_debit_note.view_move_form_debit"/>
            <field name="arch" type="xml">
                <xpath expr="//button[@name='%(account_debit_note.action_view_account_move_debit)d']"
                       position="attributes">
                    <attribute name="string">Adicionar Nota de Débito</attribute>
                </xpath>
            </field>
        </record>

        <record id="action_debit_note_customer" model="ir.actions.act_window">
            <field name="name">Notas de Débito (Clientes)</field>
            <field name="res_model">account.move</field>
            <field name="domain">[
                ('is_debit_note','=',True),
                ('debit_origin_id','!=',False),
                ('move_type','=','out_invoice')
                ]
            </field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_move_tree_debit_note"/>
            <field name="context">{'default_move_type': 'out_invoice', 'default_is_debit_note': True}</field>
        </record>

        <record id="action_debit_note_vendor" model="ir.actions.act_window">
            <field name="name">Notas de Débito (Fornecedores)</field>
            <field name="res_model">account.move</field>
            <field name="domain">[
                ('is_debit_note','=',True),
                ('debit_origin_id','!=',False),
                ('move_type','=','in_invoice')
                ]
            </field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="view_move_tree_debit_note"/>
            <field name="context">{'default_move_type': 'in_invoice', 'default_is_debit_note': True}</field>
        </record>

        <record id="account.action_move_out_invoice_type" model="ir.actions.act_window">
            <field name="domain">[('move_type', '=', 'out_invoice'), ('is_debit_note', '=', False)]</field>
        </record>

        <record id="account.action_move_in_invoice_type" model="ir.actions.act_window">
            <field name="domain">[('move_type','=','in_invoice'), ('is_debit_note','=',False)]</field>
        </record>


        <record id="l10n_ao_debit_note_account_move_view" model="ir.ui.view">
            <field name="name">Account Move View Debit Note Inherit</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account_debit_note.view_move_form_debit"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='debit_origin_id']/following-sibling::button[@type='action']"
                       position="attributes">
                    <attribute name="attrs">{'invisible': [('state', '!=', 'posted')]}</attribute>
                </xpath>
            </field>
        </record>

        <menuitem id="menu_nota_debito" name="Notas de Débito" parent="account.menu_finance_receivables"
                  action="action_debit_note_customer" sequence="3"/>
        <menuitem id="menu_debit_notes_vendors" name="Notas de Débito" parent="account.menu_finance_payables"
                  action="action_debit_note_vendor" sequence="3"/>


    </data>
</odoo>